spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:fiapfoodtest;DB_CLOSE_DELAY=-1;AUTO_RECONNECT=TRUE;MODE=MySQL;
    username: admin
    password: fiapfood
  sql:
    init:
      mode: never
  messages:
    encoding: UTF-8